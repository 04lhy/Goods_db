# 数据库系统小学期第四周周计划 — 杨玉山（物流电商数据模型 + 业务查询）

1. 编写物流电商场景完整 DDL 建表脚本 logistics_ecommerce_schema.sql——warehouses（仓库）/ products（商品）/ inventory（库存）/ customers（客户）/ orders（订单）/ order_items（订单明细）/ shipments（物流）7 张表，包含完整列定义、主键/外键/UNIQUE/NOT NULL 约束、7 个推荐索引（inventory.product_id Hash / inventory.warehouse_id Hash / orders.customer_id Hash / orders.order_date B+Tree / order_items.product_id Hash / shipments.order_id Hash / shipments.tracking_no B+Tree）的 CREATE INDEX 语句。

2. 编写批量测试数据生成器 tools/data_gen/generate_test_data.cpp——生成 50 个仓库（随机名称/地址/容量）+ 1000 种商品（随机 SKU/分类/价格）+ 20 万条库存记录 + 5 万条客户 + 10 万条订单（状态分布：pending 40% / processing 30% / shipped 20% / delivered 10%）+ 50 万条订单明细（每订单 1-8 种商品）+ 10 万条物流记录。输出为批量 INSERT SQL 文件（每 100 行一批），加载后验证数据完整性。

3. 执行全部 7 大物流电商业务查询测试：
   - 库存实时查询——按仓库+商品查库存数量和货架位置
   - 多仓库库存汇总——按仓库汇总库存总量，排序 TOP 10
   - 订单物流 JOIN——客户订单关联物流追踪信息
   - 客户消费排行——按客户消费总额排名（含会员等级）
   - 库存预警——库存量低于阈值 100 的商品列表
   - 销售趋势分析——按日汇总销售额 + 7 日滚动窗口（窗口函数）
   - 复杂 5 表 JOIN——订单→客户→商品→物流→仓库全链路查询
   每个查询记录 SQL + EXPLAIN 输出 + 执行耗时(ms) + 返回行数 + 使用的索引，输出业务查询测试报告。

4. 输出物流电商数据模型设计文档——ER 图（7 张表关系）+ 索引选型方案（每张表的索引设计 + B+Tree vs Hash 选择依据）+ 业务查询 SQL 模板库 + 数据加载脚本使用说明。
