# 数据库系统小学期第一周周计划 — 李锦华（磁盘与缓冲层）

1. 阅读 BusTub 源码中 `storage/disk/disk_manager.cpp`，理解页 ID 体系（文件号 + 页内偏移）和单文件磁盘管理模式，设计并实现 DiskManager 类，支持多文件场景（每表一个 `.db` 文件），包含文件的第 0 页元信息头设计，完成分配/读写/释放/多文件并发的单元测试。
2. 阅读 BusTub 源码中 `buffer/buffer_pool_manager.cpp` 和 `storage/page/page.h`，理解 Page 类结构和缓冲池的 Fetch/Unpin/New/Delete/Flush 生命周期，实现 BufferPoolManager（页表 + 空闲帧列表 + 互斥锁线程安全），基于王宇航提供的 Replacer 虚基类接口集成替换策略，完成并发 Fetch/Unpin 和脏页刷盘的单元测试。**周三前必须交付稳定接口给杨玉山、刘晗阳。**
3. 与王宇航的 Replacer + PageGuard 集成联调，确保 ClockReplacer / LRUKReplacer 可正常接入 BufferPoolManager，PageGuard RAII 封装正确管理页的锁与生命周期。
4. 实现缓冲池参数调优（pool_size 16/64/128/512 MB 下的 CRUD 吞吐对比）、脏页刷盘策略（定时后台刷盘 + 淘汰时刷盘），暴露 hit_rate / dirty_pages / free_frames 统计接口。
5. 与杨玉山的 TableHeap 和刘晗阳的 B+Tree 完成联调，确保 FetchPage / UnpinPage 在高并发场景下稳定工作，输出缓冲池设计文档与替换策略选择建议。
