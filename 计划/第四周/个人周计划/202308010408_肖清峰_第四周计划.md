# 数据库系统小学期第四周周计划 — 肖清峰（引擎接口层 / 管理工具 + 集成测试）

1. 实现 goods_db_admin 命令行管理工具——基于 GoodsDbClient 网络客户端库实现独立 CLI 程序，覆盖 ping / status [--extended] / version / processlist / kill <id> / create database <name> / drop database <name> / flush [hosts|logs|tables] / reload / shutdown 全部 11 个管理命令，支持 --host/--port/--user/--password/--socket 连接参数。
2. 编写 goods_db_admin 完整命令测试和自动化测试脚本（test_admin_commands.sh），输出 goods_db_admin 文档（每个命令的用法/参数/输出示例 + 与 mysqladmin 功能对照表 + 典型管理操作场景示例）。
3. 编写全系统端到端集成测试——覆盖 10 大场景：服务器启动→客户端连接→DDL 建表→DML 数据操作→复杂查询→事务→权限→日志→备份恢复→服务器关闭重启数据持久化，每个场景输出 PASS/FAIL 和详细日志。
4. 编写自动化集成测试脚本 test/integration/full_system_test.sh 全自动执行 10 大场景 50+ 测试用例，覆盖边界情况（空表操作/超长 VARCHAR/极端数值/错误处理/并发一致性/崩溃恢复）。
5. 组织全组最终验收演示——按验收脚本完整展示 goods_db 全栈能力（编译启动 → DDL → 数据加载 → 业务查询 → 事务并发 → 安全管理 → 备份恢复 → 日志系统 → 管理工具 → 桌面客户端），记录反馈和改进清单。
6. 输出阶段四全系统集成测试最终报告——所有测试用例执行结果 + 覆盖模块和代码路径 + 已修复 Bug 记录 + 残余风险点列表。
