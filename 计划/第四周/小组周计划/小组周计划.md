# goods_db 小组第四周计划

### 总目标：

1. 基于第三阶段完成的完整数据库服务器，实现运维工具层（设计三 goods_db_admin + 设计四 goods_db_dump），并完成桌面客户端 goods_db_studio 的完善整合。
2. 将 goods_db_admin（数据库管理）、goods_db_dump（备份恢复）功能从 CLI 工具整合为桌面客户端 GUI 面板，实现"一个界面管理一切"的统一体验。
3. 实现新零售场景完整数据模型（7 张表）的 DDL 建表、批量数据加载（50 万+ 行）、业务查询测试（7 大场景），验证全系统端到端正确性和性能。
4. 完成全系统性能测试（Sysbench 风格并发压测、Buffer Pool 命中率分析、索引性能对比、全表扫描 vs 索引查询基准），输出性能测试报告。
5. 交付完整项目——编译零警告、全量测试通过、桌面客户端功能完整、新零售业务场景全部可执行、设计文档齐全。

---

### 成员分工总览

| 成员 | 负责模块 | 模块说明 |
|------|---------|---------|
| **刘晗阳（组长）** | 桌面客户端完善整合（设计十）★★★★ | 管理面板 + 备份恢复向导 + 日志查看器 + 用户管理界面 + UI 体验打磨 |
| **肖清峰** | 管理工具 CLI + 集成测试（设计三）★★★ | goods_db_admin 命令行工具 + 全系统端到端集成测试 + 验收演示 |
| **李锦华** | 备份子系统（设计四）★★★ + 桌面端辅助 | goods_db_dump 全量/增量备份 + 桌面端备份恢复向导对接 + 辅助 |
| **杨玉山** | 新零售数据模型 + 业务查询 ★★ | DDL 建表脚本 + 批量数据生成 + 7 大业务场景查询测试 |
| **唐瑞涛** | 性能测试 + 项目文档 + CI/CD ★★ | Sysbench 压测框架 + 性能基准报告 + 全部文档汇总 + 最终 CI 验收 |
| **王宇航** | 并发性能测试 + 系统调优 ★★ | 并发压力测试 + Buffer Pool 命中率分析 + 索引性能对比 + 协助集成 |

---

### 本周关键接口交付里程碑

| 时间 | 提供方 | 接口 | 消费方 | 说明 |
|------|--------|------|--------|------|
| 周一晚 | 杨玉山 | 新零售 DDL 建表脚本 + 测试数据生成器 | 全员 | 后续所有测试的数据基础 |
| 周二中 | 肖清峰 | `goods_db_admin` 全部命令可用 | 刘晗阳 | 桌面端管理面板功能映射 |
| 周二晚 | 李锦华 | `goods_db_dump` 全量备份 | 刘晗阳 | 桌面端备份向导对接 |
| 周三中 | 刘晗阳 | 桌面端管理面板 + 备份恢复向导 | 全员 | 桌面端功能趋于完整 |
| 周三晚 | 李锦华 | `goods_db_dump` 增量备份（基于 binlog 位点） | 全员 | 备份子系统完整闭环 |
| 周四中 | 刘晗阳 | 桌面端日志查看器 + 用户管理界面 | 全员 | 桌面端全部功能就绪 |
| 周四晚 | 王宇航 | 性能测试报告（并发/缓存/索引） | 全员 | 性能数据汇总 |
| 周五中 | 肖清峰 | 全系统集成测试全部通过 | 全员 | ★ 项目最终验收 |
| 周五晚 | 全员 | 完整项目交付（代码 + 文档 + 演示） | — | ★ 最终交付 |

---

### Date：2025.9.7 星期日

**学习计划：**
启动阶段四运维工具与新零售集成开发。杨玉山编写新零售 DDL 建表脚本和批量数据生成器，这是本周所有集成测试的数据基础；肖清峰实现 goods_db_admin 核心管理命令；李锦华实现 goods_db_dump 全量备份基本功能；刘晗阳搭建桌面端管理面板布局并整合已有 CLI 命令功能；唐瑞涛搭建性能测试框架；王宇航准备并发压测方案和数据采集工具。

**学习概要：**
第四周是项目收官周——运维工具 + 桌面整合 + 新零售集成 + 性能测试四线并行。杨玉山的数据生成器是全员的前置依赖，周一必须交付。肖清峰的 admin 和 李锦华的 dump 是桌面端管理面板和备份向导的后端支撑。各模块本周都需要在周五前完成文档和验收准备。

**杨玉山负责：**
（上午）编写新零售场景完整 DDL 建表脚本 `new_retail_schema.sql`：
```sql
-- 仓库表
CREATE TABLE warehouses (
    id INT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    location VARCHAR(200) NOT NULL,
    capacity INT NOT NULL,
    status VARCHAR(20) DEFAULT 'active'
);
-- 商品表
CREATE TABLE products (
    id INT PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    sku VARCHAR(50) UNIQUE NOT NULL,
    category VARCHAR(50) NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL
);
-- 库存表
CREATE TABLE inventory (
    warehouse_id INT NOT NULL,
    product_id INT NOT NULL,
    quantity INT NOT NULL DEFAULT 0,
    shelf_location VARCHAR(50),
    last_updated TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY (warehouse_id, product_id)
);
-- 客户表
CREATE TABLE customers (
    id INT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    address VARCHAR(300),
    phone VARCHAR(20),
    membership_level VARCHAR(20) DEFAULT 'normal'
);
-- 订单表
CREATE TABLE orders (
    id INT PRIMARY KEY,
    customer_id INT NOT NULL,
    order_date TIMESTAMP DEFAULT NOW(),
    status VARCHAR(20) DEFAULT 'pending',
    total_amount DECIMAL(12,2) NOT NULL
);
-- 订单明细表
CREATE TABLE order_items (
    order_id INT NOT NULL,
    product_id INT NOT NULL,
    quantity INT NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL,
    PRIMARY KEY (order_id, product_id)
);
-- 物流表
CREATE TABLE shipments (
    id INT PRIMARY KEY,
    order_id INT NOT NULL,
    warehouse_id INT NOT NULL,
    status VARCHAR(20) DEFAULT 'processing',
    tracking_no VARCHAR(50) UNIQUE,
    ship_date TIMESTAMP,
    arrive_date TIMESTAMP
);
```
（下午）编写批量测试数据生成器 `tools/data_gen/generate_test_data.cpp`——生成 50 个仓库（随机名称/地址/容量）、1000 种商品（随机 SKU/分类/价格）、20 万条库存记录（随机仓库-商品组合 + 随机数量 0-10000）、5 万条客户记录（随机姓名/地址/电话）、10 万条订单（随机客户 + 状态分布：pending 40% / processing 30% / shipped 20% / delivered 10%）、50 万条订单明细（每个订单 1-8 种商品）、10 万条物流记录。数据生成后输出为 `.sql` 文件（INSERT 语句批量格式：`INSERT INTO table VALUES (row1), (row2), ... (row100)` 每 100 行一批，加快加载速度）。（晚间）在 goods_db 上执行建表脚本 → 加载全部测试数据 → 验证数据完整性（各表行数校验、索引可用性验证、Catalog 元数据一致性检查）。

**肖清峰负责：**
（上午）实现 `goods_db_admin` 命令行管理工具——基于第三阶段的网络客户端库（GoodsDbClient），实现独立 CLI 程序：`goods_db_admin [options] command [args]`。实现以下命令：
- `ping` → 连接服务器并检查存活
- `status [--extended]` → 服务器状态（运行时间、QPS、连接数、缓冲池命中率、脏页数）
- `version` → goods_db 服务器版本号
- `processlist` → 列出所有活跃连接（ID / 用户 / 主机 / 数据库 / 命令 / 时间 / 状态）
- `kill <connection_id>` → 终止指定连接
- `create database <name>` → 创建数据库（发送 `CREATE DATABASE name` SQL）
- `drop database <name>` → 删除数据库
- `flush [hosts|logs|tables]` → 清除缓存/日志/表
- `reload` → 重载权限表（发送 `FLUSH PRIVILEGES`）
- `shutdown` → 关闭服务器（需确认）
（下午）实现 `goods_db_admin` 的服务器状态报告功能——通过网络协议向服务器请求统计信息（QPS/连接数/命中率等），如果服务器尚未暴露统计接口，则协调唐瑞涛在服务器端添加 `SHOW STATUS` SQL 命令。（晚间）编写 goods_db_admin 完整命令测试（每种命令的输入输出验证）和与服务器的联调测试。

**李锦华负责：**
（上午）实现 `goods_db_dump` 全量备份工具——命令行程序：`goods_db_dump [options] [database [table...]]`。核心流程：
1. 连接服务器，获取 Catalog（所有数据库和表的列表）
2. 对每张选定的表：
   a. 发送 `SHOW CREATE TABLE table_name` → 获取完整 DDL（或通过 Catalog 构建 CREATE TABLE 语句）
   b. 发送 `SELECT * FROM table_name` → 获取全部数据行
   c. 生成 `INSERT INTO table_name VALUES (row1), (row2), ...` 批量 INSERT 语句
3. 写入输出文件（默认 stdout，`--result-file=path` 指定文件）
4. 选项：`--no-create-info`（不导出 CREATE TABLE）、`--no-data`（仅导出结构）、`--complete-insert`（INSERT 包含列名）、`--single-transaction`（在事务中导出保证一致性）
（下午）实现备份进度显示——大表备份时输出当前行数/总行数和进度百分比。实现恢复功能：`goods_db_dump` 生成的 `.sql` 文件可通过 `Execute(sql_file_content)` 进行恢复（利用第二阶段 SQL 解析器和执行器，或直接通过 goods_db shell 执行 SQL 文件）。（晚间）编写全量备份/恢复的端到端测试——建表 → 插入 1 万行 → dump → DROP TABLE → 从 dump 文件恢复 → 验证数据完全一致。

**刘晗阳（组长）负责：**
（上午）实现桌面端管理面板 `AdminPanel`——在主窗口新增"管理"标签页或在工具菜单中打开管理面板。面板布局：上半部分为服务器状态仪表盘（QPS 数值 + 连接数 + 缓冲池命中率百分比 + 脏页数 + 服务器运行时间），使用 QGroupBox + QGridLayout 布局，数值使用 QLabel 大字体显示。下半部分为进程列表（QTableView + 自定义 Model 绑定 `SHOW PROCESSLIST` 结果），包含列：ID / 用户 / 主机 / 数据库 / 命令 / 时间(秒) / 状态。选中某行后可点击"Kill"按钮终止该连接（发送 `KILL id` 命令）。（下午）实现管理面板中的操作按钮组——Flush Hosts / Flush Logs / Flush Tables 三个按钮（调用 `FLUSH ...` SQL）、Reload 按钮（调用 `FLUSH PRIVILEGES`）、Ping 按钮（心跳检测）、Shutdown 按钮（确认对话框 → 发送 shutdown 命令）。实现定时刷新机制（QTimer，每 5 秒自动刷新状态指标和进程列表）。（晚间）与肖清峰联调——确保管理面板通过 GoodsDbClient 调用 `goods_db_admin` 对应命令正确，状态数据实时更新。

**唐瑞涛负责：**
（上午）搭建性能测试框架——基于 Google Test 编写性能 Benchmark 测试用例集：`benchmark_full_table_scan(n_rows)` / `benchmark_index_point_query(n_rows, n_queries)` / `benchmark_index_range_scan(n_rows, range_size)` / `benchmark_join(n_left, n_right)` / `benchmark_aggregation(n_rows, n_groups)` / `benchmark_concurrent_reads(n_clients, n_queries)` / `benchmark_concurrent_writes(n_clients, n_rows)`。每个 Benchmark 函数执行多次 warm-up 后取 P50/P95/P99 耗时。（下午）实现性能指标采集——在服务器端暴露统计接口：`SHOW STATUS` → 返回包含 QPS（Queries Per Second）、连接数、缓冲池命中率、脏页数、锁等待数、死锁数、网络收发包数等指标的结构化数据。`SHOW ENGINE goods_engine STATUS` → 返回存储引擎特定指标（BufferPool 各帧状态、索引页缓存命中率、TableHeap 统计）。（晚间）编写第一个 Benchmark 测试用例（10 万行全表扫描）验证框架可用，输出 Timing 数据格式和报告模板。

**王宇航负责：**
（上午）设计并发压力测试方案——Sysbench 风格的多场景压测：
1. **只读压测**：N 个客户端并发执行 `SELECT * FROM table WHERE id = ?`（随机 id 点查）
2. **读写混合压测**：N 个客户端按 8:2 比例混合执行 SELECT 和 UPDATE
3. **写入压测**：N 个客户端并发 INSERT + DELETE 操作
4. **复杂查询压测**：N 个客户端并发执行 3 表 JOIN + 聚合查询
5. **热点竞争压测**：全部客户端并发 UPDATE 同一行数据
每种场景测试 50/100/200 三种并发度，每种跑 60 秒，采集吞吐量（QPS）、平均延迟、P95/P99 延迟、错误率（死锁回滚率）。（下午）编写压测客户端程序 `tools/benchmark/stress_client.cpp`——接收场景配置（并发数、操作类型、运行时长），spawn N 个线程，每个线程独立连接服务器，按场景执行 SQL 操作，记录每次操作的耗时到 CSV 文件（线程 ID / 操作类型 / 开始时间 / 耗时 ms / 是否成功）。（晚间）进行首轮压测（50 并发，只读场景，10 万行数据）采集基线数据，验证压测框架正确性。

---

### Date：2025.9.8 星期一

**学习计划：**
肖清峰交付 goods_db_admin 全部命令并通过验收（★ 关键里程碑）；李锦华交付 goods_db_dump 全量备份功能；刘晗阳实现桌面端备份恢复向导对接李锦华的 dump 功能；杨玉山完成新零售数据加载与基本链路验证；唐瑞涛完成 Benchmark 测试套件；王宇航完成全部压测场景并采集基线数据。

**学习概要：**
今天运维工具层集中交付——肖清峰的 admin 和李锦华的 dump 完成后，刘晗阳即可将两者整合到桌面客户端 GUI 面板中，实现"桌面端一个界面完成管理和备份"。杨玉山的新零售数据就绪后，全组可在此基础上进行性能测试。

**肖清峰负责：**
（上午）收尾 goods_db_admin 全部命令的单元测试和服务器联调测试。实现 admin 工具的连接参数支持——`--host=HOST` / `--port=PORT` / `--user=USER` / `--password=PASSWORD`，默认从 `goods_db.conf` 读取。支持 `--socket=SOCKET`（本地 Unix Socket 连接）、`--compress`（压缩协议传输）、`--connect-timeout=N`（连接超时秒数）。（下午）输出 goods_db_admin 完整文档——每个命令的用法/参数/输出示例、与 mysqladmin 的功能对照表（记录 goods_db_admin 已实现 vs 暂未实现的命令）、典型管理操作场景示例（排查服务器问题、清理缓存、终止慢查询）。编写 admin 工具的 shell 自动化测试脚本（`test_admin_commands.sh`）覆盖所有命令。（晚间）与刘晗阳联调——确保桌面端管理面板调用的底层命令与 goods_db_admin CLI 行为一致。

**李锦华负责：**
（上午）收尾 goods_db_dump 全量备份单测。实现增量备份功能——基于 Binary Log 位点：记录上次备份时的 binlog 文件名和 position → 下次备份时从上次位点开始读取 binlog 事件 → 将 QUERY 事件（INSERT/UPDATE/DELETE）转换为 SQL 语句输出。`goods_db_dump --incremental --since-position=binlog.000003:12345` → 增量备份 SQL 文件。（下午）实现备份一致性保证——`--single-transaction` 选项：开启 REPEATABLE READ 事务 → 在事务内导出全部数据，保证导出的是某一时刻的一致性快照（与其他并发 DML 互不干扰）。输出 goods_db_dump 完整文档——全量/增量/恢复的操作流程、备份文件格式说明（SQL 脚本的结构 + 注释规范）、与 mysqldump 的功能对照表、备份恢复最佳实践（备份频率建议、备份文件安全存储建议）。（晚间）编写增量备份端到端测试——全量备份 → 执行一批 INSERT/UPDATE → 增量备份 → 全量恢复 → 增量恢复 → 验证数据与最新状态一致。与刘晗阳联调桌面端备份恢复向导。

**刘晗阳（组长）负责：**
（上午）实现桌面端备份恢复向导 `BackupWizard`——QWizard（向导风格对话框）分步骤页面：
- Page 1（选择操作）：全量备份 / 增量备份 / 恢复
- Page 2a（全量备份）：选择数据库/表（QTreeWidget 带复选框）→ 选择导出路径（QFileDialog）→ 选项（是否导出 CREATE TABLE / 是否导出列名 / 每批 INSERT 行数）
- Page 2b（增量备份）：显示上次备份位点 → 选择导出路径 → 开始
- Page 2c（恢复）：选择 `.sql` 备份文件 → 预览前 20 行（QTextEdit 只读）→ 选择是否在事务中恢复
- Page 3（执行）：点击"开始"按钮 → 进度条 + 实时日志输出（QPlainTextEdit 追加模式）→ 完成显示耗时和文件大小
（下午）将备份恢复向导与李锦华的 goods_db_dump 对接——备份操作：调用 `GoodsDbClient::Execute("SELECT ...")` 获取数据 → 在桌面端本地生成备份 SQL 文件（不通过服务器端 dump 命令，而是桌面端直接生成，避免备份文件先写到服务器再传回）。恢复操作：读取备份 SQL 文件 → 分条发送到服务器执行 → 进度跟踪。（晚间）编写备份恢复向导的集成测试——全量备份 → 删除表 → 恢复 → 数据一致性验证全流程。

**杨玉山负责：**
（上午）收尾新零售场景数据加载与基本验证——确保 7 张表和 50 万+ 行数据正确加载到 goods_db，验证每张表的行数和关键字段。创建推荐索引：
- inventory 表：`CREATE INDEX idx_inv_product ON inventory (product_id) USING HASH`（按商品查库存）
- inventory 表：`CREATE INDEX idx_inv_warehouse ON inventory (warehouse_id) USING HASH`（按仓库查库存）
- orders 表：`CREATE INDEX idx_orders_customer ON orders (customer_id) USING HASH`（按客户查订单）
- orders 表：`CREATE INDEX idx_orders_date ON orders (order_date) USING BTREE`（按日期范围查订单）
- order_items 表：`CREATE INDEX idx_oi_product ON order_items (product_id) USING HASH`
- shipments 表：`CREATE INDEX idx_ship_order ON shipments (order_id) USING HASH`（物流 JOIN 订单）
- shipments 表：`CREATE INDEX idx_ship_tracking ON shipments (tracking_no) USING BTREE`（按运单号查询）
（下午）编写新零售业务查询测试用例集（7 大场景），每个查询记录：SQL 语句、EXPLAIN 输出（使用的索引和执行计划）、执行耗时（执行 5 次取中位数）、返回行数。输出新零售业务查询测试报告（`docs/new_retail_queries.md`）。（晚间）与唐瑞涛对接——将业务查询测试用例转化为 Benchmark 测试用例，加入性能测试套件。

**唐瑞涛负责：**
（上午）完成全部 Benchmark 测试用例的编码——所有场景覆盖：全表扫描（1 万/10 万/100 万行）、B+Tree 等值点查、B+Tree 范围扫描（100/1000/10000 行）、Hash 等值点查、2 表 HashJoin（1000×10000 / 10000×100000）、3 表级联 Join、GROUP BY 聚合（10/100/1000 分组）、ORDER BY + LIMIT 排序、窗口函数 ROW_NUMBER/RANK。每个用例跑 5 次 warm-up + 10 次正式测试，输出中位数和标准差。（下午）更新 CI 流水线——增加 Benchmark 编译目标（可选，不阻塞 CI），增加性能回归检测（当前版本 vs 上一版本的 Benchmark 中位数对比，关键查询性能退化 > 10% 时报 warn）。输出 CI/CD 完整配置文件文档（`.github/workflows/` 下所有 workflow 的说明）。（晚间）进行一轮完整 Benchmark 测试，输出性能基线报告初稿。

**王宇航负责：**
（上午）完成全部 5 种压测场景的自动化脚本——每个场景 3 种并发度（50/100/200）共 15 组测试，每组跑 60 秒，自动采集 QPS、延迟分布、错误率。编写压测结果汇总脚本 `tools/benchmark/aggregate_results.py`——读取各组 CSV 数据 → 计算 QPS / AVG / P50 / P95 / P99 / MAX 延迟 / 错误率 → 生成 Markdown 表格和简易 ASCII 图表。（下午）进行首轮完整压测——从 50→100→200 并发依次执行 5 大场景。进行 Buffer Pool 命中率分析——固定 pool_size 参数（16/64/128/256 MB 四档），每档下跑只读压测，采集命中率 → 输出"命中率 vs pool_size vs 并发数"三维对比表。（晚间）输出并发性能测试报告初稿——含 QPS-并发数曲线、延迟分布百分位图（P50/P95/P99）、Buffer Pool 命中率-吞吐量相关性分析、锁争用热力图。

---

### Date：2025.9.9 星期二

**学习计划：**
刘晗阳交付桌面端管理面板 + 备份恢复向导 + 日志查看器（★ 桌面端功能趋于完整）；李锦华交付增量备份（基于 binlog 位点）；杨玉山完成全部 7 大业务查询测试并输出报告；唐瑞涛完成全量性能基准测试；王宇航完成并发压测收尾与性能瓶颈分析；肖清峰启动全系统端到端集成测试。

**学习概要：**
今天桌面端功能井喷——管理面板、备份向导、日志查看器三个核心工具面板全部完成。杨玉山的业务查询测试数据是性能报告的核心内容，唐瑞涛和王宇航分别从单查询和并发维度采集完整的性能数据。肖清峰今天启动全系统集成测试，为周五最终验收做好准备。

**刘晗阳（组长）负责：**
（上午）实现桌面端日志查看器 `LogViewer`——在主窗口"工具"菜单或底部 Dock 中打开。三个标签页（QTabWidget）：Error Log / Query Log / Binary Log。每个标签页包含：
- 顶部过滤栏：日志级别下拉（Error Log）/ 用户过滤（Query Log）/ 时间范围选择（QDateTimeEdit ×2）→ 刷新按钮
- 中间内容区：QTableView（Error/Query Log 按列展示）或 QPlainTextEdit 只读模式（Binary Log，带 ANSI 颜色高亮不同类型事件）
- 底部搜索栏：QLineEdit + 上一个/下一个按钮 + 高亮匹配项数
日志数据获取方式——Error Log 和 Query Log 通过向服务器发送 `SHOW LOGS` + 读取日志内容命令（新增 `SHOW ERRORLOG [LIMIT n]` 和 `SHOW QUERYLOG [LIMIT n]` 服务器端接口），Binary Log 通过调用 `goods_db_binlog` 工具的输出解析或通过服务器端 `SHOW BINLOG EVENTS` 接口。
（下午）实现桌面端用户管理界面 `UserManager`——在主窗口"工具"菜单中打开。QWidget 布局：
- 上半部分：用户列表 QTableView（用户名 / 主机 / 创建时间）
- 按钮：新建用户（QDialog：用户名+密码+主机+确认密码） / 删除用户（确认对话框） / 修改密码（QDialog：旧密码+新密码+确认新密码）
- 下半部分：权限编辑区域（选中某用户后显示）——左侧 QTreeWidget（数据库→表层次），右侧权限勾选矩阵（SELECT/INSERT/UPDATE/DELETE/CREATE/DROP/INDEX/GRANT 的 QCheckBox），"应用权限"按钮发送 GRANT/REVOKE 命令
（晚间）实现 UI 体验打磨——快捷键配置对话框（QDialog + QTableWidget 列出所有操作→快捷键映射，支持双击修改和重置默认值）、窗口布局保存/恢复（QSettings 持久化各 Dock 位置和大小，下次启动自动恢复）、国际化框架（中文 `goods_db_zh_CN.ts` + 英文 `goods_db_en_US.ts` 的 Qt Linguist 格式，实现 >50 个 UI 字符串的翻译覆盖）。

**肖清峰负责：**
（上午）启动全系统端到端集成测试——编写完整测试用例覆盖 goods_db 所有功能链路：
1. 服务器启动 → 网络监听验证
2. 客户端连接 → 用户认证（成功 + 失败场景）
3. DDL 建表（CREATE TABLE + CREATE INDEX）→ Catalog 验证
4. DML 数据操作（INSERT × 10000 + SELECT + UPDATE + DELETE）→ 数据一致性验证
5. 复杂查询（JOIN + 聚合 + 排序 + 窗口函数 + 子查询）→ 结果正确性验证
6. 事务（BEGIN → DML → COMMIT/ROLLBACK）→ 隔离级别验证
7. 权限（GRANT → 操作 → REVOKE → 拒绝操作）→ 权限生效验证
8. 日志（所有操作记录在 Error/Query/Binlog）→ 日志完整性验证
9. 备份恢复（全量 dump → DROP → 恢复 → 一致性验证）
10. 服务器优雅关闭 → 重启 → 数据持久化验证
（下午）编写自动化集成测试脚本 `test/integration/full_system_test.sh`——Shell 脚本自动执行以上 10 个测试场景，每个场景输出 PASS/FAIL 和详细日志。（晚间）执行第一轮完整集成测试，记录失败用例和修复计划。

**李锦华负责：**
（上午）收尾增量备份功能——基于 binlog 位点的增量备份完整实现：`SHOW MASTER STATUS` 获取当前位点 → 记录到 `goods_db_dump.pos` 文件 → 下次备份时读取上次位点 → `mysqlbinlog --start-position=LAST_POS` 获取增量 SQL。测试增量备份与全量备份的组合恢复：全量备份（周一）→ 增量备份 1（周二）→ 增量备份 2（周三）→ DROP ALL → 全量恢复 → 增量恢复 1 → 增量恢复 2 → 数据与周三状态一致。（下午）输出备份子系统完整设计文档：全量备份流程图（Catalog 遍历 → SELECT 数据 → 生成 SQL 脚本）、增量备份流程图（读取 binlog → 事件过滤 → 转换为 SQL → 输出）、恢复流程图（解析 SQL 文件 → 按序执行 → 错误处理）。备份策略建议：全量备份频率（建议每日凌晨） + 增量备份频率（建议每小时）+ 备份文件轮转策略。（晚间）与刘晗阳完成桌面端备份恢复向导的完整联调测试。

**杨玉山负责：**
（上午）执行全部 7 大新零售业务查询测试，每查询记录：
1. 库存实时查询——`SELECT i.quantity, i.shelf_location FROM inventory i WHERE warehouse_id = 10 AND product_id = 500`
2. 多仓库库存汇总——`SELECT w.name, SUM(i.quantity) AS total FROM inventory i JOIN warehouses w ON i.warehouse_id = w.id GROUP BY w.id ORDER BY total DESC LIMIT 10`
3. 订单+物流 JOIN——`SELECT o.id, o.status, o.total_amount, s.tracking_no, s.status AS ship_status FROM orders o LEFT JOIN shipments s ON o.id = s.order_id WHERE o.customer_id = 1000 ORDER BY o.order_date DESC LIMIT 20`
4. 客户消费排行——`SELECT c.name, c.membership_level, SUM(oi.quantity * oi.unit_price) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id GROUP BY c.id ORDER BY total_spent DESC LIMIT 20`
5. 库存预警——`SELECT p.name AS product, w.name AS warehouse, i.quantity FROM inventory i JOIN products p ON i.product_id = p.id JOIN warehouses w ON i.warehouse_id = w.id WHERE i.quantity < 100 ORDER BY i.quantity ASC LIMIT 50`
6. 销售趋势分析——`SELECT DATE(o.order_date) AS day, SUM(o.total_amount) AS daily_sales, SUM(SUM(o.total_amount)) OVER (ORDER BY DATE(o.order_date) ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS rolling_7d_sales FROM orders o GROUP BY DATE(o.order_date) ORDER BY day`
7. 复杂 5 表 JOIN——`SELECT o.id, c.name, p.name, s.tracking_no, w.name FROM orders o JOIN customers c ON o.customer_id = c.id JOIN order_items oi ON o.id = oi.order_id JOIN products p ON oi.product_id = p.id LEFT JOIN shipments s ON o.id = s.order_id LEFT JOIN warehouses w ON s.warehouse_id = w.id WHERE o.order_date >= '2025-06-01' LIMIT 100`
（下午）输出新零售业务查询测试报告——每个查询的 SQL + EXPLAIN 输出 + 实际执行耗时（ms）+ 返回行数 + 使用的索引列表 + 性能评估（OK/需要优化）。标注性能瓶颈查询（如查询 7 的 5 表 JOIN 耗时过高），提出优化建议（增加合适索引、改写 SQL、调整 JOIN 顺序）。（晚间）将新零售业务测试数据提交给唐瑞涛汇总到项目最终报告。

**唐瑞涛负责：**
（上午）完成全量性能基准测试——所有 Benchmark 用例在 1 万/10 万/100 万三种数据量下运行，采集 P50/P95/P99 耗时。输出基准报告表格如下（示例）：
| 操作 | 1万行 | 10万行 | 100万行 | 使用索引 | 复杂度 |
|------|-------|--------|---------|----------|--------|
| 全表扫描 | 12ms | 98ms | 950ms | 无 | O(n) |
| B+Tree 点查 | 0.3ms | 0.5ms | 0.8ms | B+Tree | O(log n) |
| Hash 点查 | 0.2ms | 0.3ms | 0.5ms | Hash | O(1) |
| B+Tree 范围(100行) | 0.8ms | 1.2ms | 2.1ms | B+Tree | O(log n + k) |
| 2表 HashJoin | 45ms | 430ms | 4400ms | Hash | O(m+n) |
| GROUP BY(10组) | 18ms | 150ms | 1400ms | 无 | O(n) |
| ORDER BY+LIMIT 100 | 15ms | 120ms | 3800ms | TopN | O(n log k) |
| 窗口函数(RANK) | 28ms | 280ms | 2900ms | 无 | O(n log n) |
（下午）汇总全组文档——收集杨玉山/肖清峰/李锦华/刘晗阳/王宇航各模块的设计文档和测试报告，整合为统一项目交付文档。编写项目总述（项目定位、技术选型、架构全景、模块清单、测试覆盖率、性能指标汇总）。输出 `docs/final_report/` 完整交付文档目录。（晚间）确保 CI 全绿——全量编译 + 200+ 单测 + 50 并发集成测试 + Benchmark + format/lint/clang-tidy 全部通过。

**王宇航负责：**
（上午）完成全场景并发压测——5 种场景 × 3 种并发度 × 5 次重复 = 75 组数据，汇总到压测报告。输出性能瓶颈分析：①锁争用瓶颈——热点行竞争场景下死锁率随并发度升高的趋势分析；②Buffer Pool 瓶颈——命中率随 pool_size 变化的拐点分析（找到性价比最高的 pool_size）；③网络 IO 瓶颈——QPS 上限的瓶颈分析（是 CPU 计算密集还是网络 IO 密集）。（下午）进行索引选型验证——在 50 万行新零售数据上，对比 B+Tree vs Hash 索引在真实业务查询中的表现：查询 1（库存点查，Hash 胜）、查询 2（仓库汇总，Hash 适合 GROUP BY 键）、查询 6（日期范围+窗口函数，B+Tree 必须），输出"新零售场景索引选型建议"最终版。（晚间）输出并发性能测试最终报告 + 索引选型建议 + 性能调优指南（针对不同业务场景的配置推荐）。

---

### Date：2025.9.10 星期三

**学习计划：**
刘晗阳完成桌面端全部功能收尾与 Bug 修复（★ 桌面端全部就绪）；肖清峰完成全系统集成测试全部通过；李锦华完成备份恢复全链路闭环测试；杨玉山完成新零售场景测试收尾与文档；唐瑞涛完成项目全部文档整合与 CI 最终验收；王宇航完成性能测试报告收尾与系统调优建议。

**学习概要：**
倒数第二天，所有功能模块收尾。上午各模块进行最后的问题修复，下午进行全系统集成测试验证。唐瑞涛开始整合全部文档为统一交付物。桌面端今天完成最后打磨——主题/快捷键/国际化/窗口布局等体验优化。

**刘晗阳（组长）负责：**
（上午）桌面端全功能收尾——管理面板（状态仪表盘实时刷新验证、Flush/Kill 操作验证、Shutdown 流程验证）、备份恢复向导（全量备份→恢复全流程验证、增量备份→恢复验证、大表备份进度条流畅性验证）、日志查看器（Error/Query/Binlog 三个标签数据展示正确性、过滤和搜索功能验证）、用户管理界面（创建/删除用户、权限勾选与 GRANT/REVOKE 对应关系验证、修改密码验证）。（下午）UI/UX 全面检查与修复：
- 深色/浅色主题全面测试——确保所有自定义控件（QPlainTextEdit、QTableView、QTreeView、QDockWidget、QTabWidget、QDialog）在两个主题下渲染正常
- 快捷键冲突检测与修复——确保 F5（执行）、Ctrl+T（新建标签）、Ctrl+W（关闭标签）、Ctrl+O（打开文件）、Ctrl+S（保存）等不冲突
- 窗口布局保存/恢复测试——拖拽 Dock 改变布局 → 关闭 → 重启 → 验证布局恢复一致
- 国际化切换测试——中文/英文切换后所有菜单/按钮/标签/提示文本正确显示
- 错误处理体验——服务器断连时的友好提示（状态栏变红 + "连接已断开" 弹窗 + 自动重连选项）、SQL 语法错误的错误消息展示、权限拒绝的错误消息展示
（晚间）输出桌面客户端完整用户手册（`docs/goods_db_studio_manual.md`）——含截图和操作步骤：安装与启动、连接服务器、SQL 编辑与执行、结果浏览与导出、对象树导航、管理面板使用、备份恢复操作、日志查看、用户管理、主题切换、国际化配置。

**肖清峰负责：**
（上午）继续执行全系统集成测试——修复第一轮发现的失败用例。重点场景验证：
- 边界情况：空表所有操作、超长 VARCHAR(1024) 的读写、极端值（INT_MAX/MIN、DECIMAL 精度边界）
- 错误处理：表不存在的 DELETE、列不存在的 SELECT、类型不匹配的 INSERT、不存在的索引 CREATE INDEX
- 并发一致性：10 个客户端同时 INSERT 到同一表 → 验证行数正确且无数据损坏
- 崩溃恢复：kill -9 强制杀死服务器 → 重启 → 验证数据完整性（从 binlog 恢复未持久化的事务）
（下午）编写集成测试总结报告——列出所有测试用例（共 50+ 个场景）的执行结果、覆盖的模块和代码路径、发现的 Bug 和修复记录、仍有风险的点（列出但不阻塞交付）。输出阶段四集成测试报告。（晚间）协助刘晗阳进行桌面端全功能验收测试——确保桌面端每个功能按钮对应的后端操作都正确执行。

**李锦华负责：**
（上午）完成备份恢复全链路闭环测试——模拟生产环境灾难恢复流程：
1. 创建完整新零售数据库（7 张表 + 50 万行数据）
2. 全量备份
3. 执行一批业务操作（1000 条 INSERT + 500 条 UPDATE + 200 条 DELETE）
4. 增量备份（基于 binlog 位点）
5. 模拟灾难（删除所有 .db 文件）
6. 全量恢复 → 验证数据恢复到全量备份时的状态
7. 增量恢复 → 验证数据恢复到增量备份时的状态
8. 数据完整性校验（行数、checksum、关键字段抽样比对）
（下午）输出备份子系统最终设计文档——含完整流程图、命令参考手册、备份策略配置建议（cron 定时示例、备份文件保存策略、监控告警建议）。编写备份恢复操作手册。（晚间）收尾所有文档和测试。

**杨玉山负责：**
（上午）收尾新零售场景测试——汇总 7 大查询的详细测试结果到报告。补充边缘场景测试：NULL 值处理（LEFT JOIN 中右表无匹配的 NULL 行解析）、VARCHAR 中文编码正确处理（仓库名/客户名/地址中包含中文字符）、大结果集排序和分页性能。（下午）输出新零售场景完整文档——数据模型 ER 图（7 张表之间的关系）、索引选型方案和理由（每张表的索引设计 + B+Tree vs Hash 选择依据）、业务查询 SQL 模板库（每个业务的查询模板和参数说明）、数据加载脚本使用说明。编写面向用户的示例场景脚本——帮助验收人员理解新零售业务的"故事线"（一个完整的采购→仓储→销售→物流流程）。（晚间）协助肖清峰进行最终集成测试中新零售数据相关场景的验证。

**唐瑞涛负责：**
（上午）整合全部项目文档——将四阶段的设计文档、测试报告、接口规范、用户手册汇总到统一的交付物。目录结构：
```
docs/final_report/
├── 01_项目概述.md               # 项目背景/目标/范围/技术选型
├── 02_系统架构.md               # 总体架构图/模块依赖图/数据流图
├── 03_存储引擎设计.md            # 设计一+二（第一阶段成果）
├── 04_查询引擎设计.md            # 设计八（第二阶段成果）
├── 05_网络协议设计.md            # 设计六（第三阶段成果）
├── 06_日志子系统设计.md          # 设计五（第三阶段成果）
├── 07_安全管理设计.md            # 设计九（第三阶段成果）
├── 08_线程内存管理设计.md        # 设计七（第三阶段成果）
├── 09_数据库管理工具设计.md      # 设计三（第四阶段成果）
├── 10_备份子系统设计.md          # 设计四（第四阶段成果）
├── 11_桌面客户端设计.md           # 设计十（第三-四阶段成果）
├── 12_新零售场景设计.md          # 数据模型/业务查询
├── 13_性能测试报告.md            # Benchmark + 并发压力测试结果
├── 14_集成测试报告.md            # 全系统集成测试结果
├── 15_用户手册.md                # 安装/配置/使用指南
└── 16_附录.md                    # 代码统计/测试覆盖率/参考文献
```
（下午）编写项目概述和系统架构两章文档——重点突出 goods_db 相对于 BusTub 的扩展和创新点（handler/handlerton 引擎接口层、10 大设计模块全覆盖、新零售场景适配、Qt 6 桌面客户端）。（晚间）执行 CI 最终验收——确保 push 到 main 分支时 CI 全绿，所有编译/测试/格式检查通过。打 tag v1.0.0。

**王宇航负责：**
（上午）收尾并发性能测试报告——输出最终版：①QPS-并发数特性曲线（5 种场景各 3 种并发度的吞吐量变化）；②延迟分布百分位图（P50→P95→P99 的趋势分析）；③Buffer Pool 命中率分析（pool_size 从 16MB 到 512MB 的命中率曲线，找到拐点）；④死锁率分析（热点行竞争场景的死锁率随并发度的指数增长趋势）；⑤性能瓶颈诊断总结（当前系统的主要瓶颈、优化优先级、预期改进效果）。
（下午）编写系统调优指南——针对不同业务场景的推荐配置：
- **OLTP 高并发写入**：LRU-K Replacer（K=2）、Buffer Pool 设置为总数据量的 20%、READ COMMITTED 隔离级别、关闭慢查询日志
- **OLAP 分析查询**：Clock Replacer（顺序扫描友好）、Buffer Pool 设置为总数据量的 50%、增大外部排序阈值、启用 HashJoin
- **均衡混合负载**：LRU-K Replacer（K=3）、Buffer Pool 设置为总数据量的 30%、REPEATABLE READ 隔离级别、Query Log 记录耗时 > 100ms 的 SQL
（晚间）协助全组进行最终验收测试，负责并发场景的压测 Demo 和数据展示。

---

### Date：2025.9.11 星期四

**学习计划：**
全员最终验收与交付。唐瑞涛完成 CI 终验与 tag 发布；肖清峰组织实施全系统验收演示；杨玉山展示新零售业务场景；李锦华展示备份恢复全流程；刘晗阳展示桌面客户端完整功能 + 汇总交付物；王宇航展示性能测试报告与调优建议。下午输出全部交付物并提交。

**学习概要：**
最后一天是项目落地的里程碑。上午全组进行最终验收演示——完整展示 goods_db 从存储引擎到桌面客户端的全栈能力。下午签署交付物 Checklist，提代码打 tag，庆祝项目完成。

**唐瑞涛负责：**
（上午）CI 最终验证——确认 `main` 分支最新 commit 的 CI 全绿（编译 + 全量测试 + Benchmark + 格式检查 + 文档生成）。执行 `git tag v1.0.0 -m "goods_db v1.0.0: 完整数据库系统交付"`。编写 Git 仓库 README 补充说明（Badge：编译状态/测试覆盖率/许可证 → 指向 CI 和文档链接）。（下午）输出最终交付物 Checklist 并逐项确认：代码（src/+test/）、文档（16 份设计文档 + 用户手册）、测试（200+ 单测 + 集成测试 + Benchmark）、配置（Dockerfile + docker-compose.yml + goods_db.conf + CI workflows）、演示（演示脚本 + 录制或实时演示）。（晚间）将全部交付物归档，准备提交。

**肖清峰负责：**
（上午）组织全系统最终验收演示——按以下脚本执行：
1. **编译启动**：`cmake .. && make -j$(nproc)` 零警告编译 → 启动 goods_db 服务器（展示启动日志和监听端口）
2. **DDL 建表**：执行新零售 7 张表 CREATE TABLE + 7 个 CREATE INDEX（展示建表耗时和 Catalog 变化）
3. **数据加载**：加载 50 万+ 行测试数据（展示加载速率 rows/sec 和总耗时）
4. **业务查询**：执行 7 大新零售查询（每个展示 SQL → EXPLAIN → 结果集前 10 行 → 耗时）
5. **事务并发**：两个客户端并发 UPDATE 同一行（展示锁等待和事务隔离行为）
6. **安全管理**：创建用户 → 授予权限 → 执行操作 → 撤销权限 → 操作被拒（完整权限生命周期）
7. **备份恢复**：全量备份 → DROP TABLE → 全量恢复 → 数据一致性验证
8. **日志系统**：展示 Error Log / Query Log / Binary Log 内容 → goods_db_binlog 解析
9. **管理工具**：展示 goods_db_admin（status/processlist/flush）命令输出
10. **桌面客户端**：完整 UI 操作流程（连接 → SQL 编辑 → 结果浏览 → 对象树 → 管理面板 → 备份向导 → 日志查看 → 用户管理 → 主题切换）
（下午）记录验收演示中的问题和反馈，整理为改进清单（P0 必须修复 / P1 下版本优化）。输出最终集成测试验收报告。

**李锦华负责：**
（上午）参与验收演示——负责备份恢复 Demo 和日志系统 Demo 的操作和讲解。准备备份恢复场景的"故事线"：模拟一次数据灾难 → 利用备份恢复避免损失。（下午）收尾所有文档和代码，确认所有测试通过。协助唐瑞涛完成交付物 Checklist 中与备份和日志相关的项。

**杨玉山负责：**
（上午）参与验收演示——负责新零售 DDL 建表 + 数据加载 + 业务查询 Demo 的操作和讲解。准备新零售场景的"故事线"：从仓库管理员视角 → 查看库存 → 补货 → 客户下单 → 仓库发货 → 物流跟踪 → 销售分析 → 客户排行。（下午）收尾所有文档和代码，确认全部业务查询测试通过。

**刘晗阳（组长）负责：**
（上午）参与验收演示——负责桌面客户端完整功能 Demo 的操作和讲解。桌面端演示重点：①统一入口体验（所有操作在一个界面完成，无需切换 CLI 工具）；②可视化优势（对象树直观浏览数据结构、结果表格清晰展示数据、管理仪表盘一目了然）；③生产力特性（语法高亮、查询历史、导出功能、多标签编辑）。（下午）汇总全组交付物——确认代码、测试、文档、配置文件全部就绪。编写项目总结——四阶段开发历程回顾、技术难点与解决方案、团队协作心得、未来展望。组织全组进行最终检查：
- [ ] `README.md` 更新到最新状态
- [ ] `docs/` 目录下所有设计文档完整
- [ ] 四阶段计划文档归档
- [ ] CI 全绿 + tag 已打
- [ ] 演示视频/截图（可选，加分项）
提交最终版本到 Git 仓库。

**王宇航负责：**
（上午）参与验收演示——负责事务并发 Demo 和性能测试报告讲解。并发 Demo 展示：两个桌面端窗口同时连接 → 同时开启事务 → 同时 UPDATE 同一行 → 展示锁排队和死锁检测。性能报告讲解：QPS-并发曲线图 → 系统吞吐量上限 → Buffer Pool 命中率分析 → 性能瓶颈诊断 → 调优建议。（下午）收尾所有文档和代码，确认所有并发测试和性能测试结果归档。协助刘晗阳完成最终交付物检查。

---

## 验收标准

- [ ] `cmake .. && make -j$(nproc)` 编译零警告（服务器 + 桌面客户端 + CLI 工具全部）
- [ ] `make check-format` / `make check-lint` / `make check-clang-tidy` 全部通过
- [ ] 全量单元测试通过（Google Test，目标测试数量 ≥ 200 个）
- [ ] **新零售场景验收**：
  - 7 张新零售表 DDL 建表成功
  - 50 万+ 行批量数据加载成功（各表行数校验通过）
  - 7 大业务查询全部返回正确结果（EXPLAIN 验证索引使用）
  - 查询性能满足预期（点查 < 1ms，范围扫描 < 10ms，JOIN < 5s）
- [ ] **运维工具验收**：
  - goods_db_admin：全部 11 个管理命令可用（ping/status/version/processlist/kill/create database/drop database/flush/reload/shutdown）
  - goods_db_dump：全量备份 + 增量备份（基于 binlog 位点）+ 恢复全部可用
  - goods_db_binlog：binlog 文件解析输出正确
- [ ] **桌面客户端全功能验收**：
  - 连接管理：多服务器连接/断开/切换/历史持久化
  - SQL 编辑器：多标签 + 语法高亮正确 + 代码补全 + F5/Ctrl+Enter 执行 + 查询历史
  - 结果浏览器：表格渲染正确 + 分页（>1000 行）+ 导出 CSV/JSON/SQL INSERT 正确
  - 对象浏览器：数据库→表→列/索引树形导航 + 双击查询 + 右键菜单
  - 管理面板：状态仪表盘实时刷新 + 进程列表 + Kill/Flush/Reload/Shutdown 操作
  - 备份恢复向导：全量备份→恢复全流程 + 增量备份→恢复全流程 + 进度条和日志
  - 日志查看器：Error/Query/Binlog 三栏切换 + 过滤 + 搜索高亮
  - 用户管理界面：创建/删除用户 + 权限勾选授予/撤销
  - 主题切换：深色/浅色无渲染异常
  - 国际化：中/英文界面切换完整覆盖
  - 窗口布局：保存/恢复正确
- [ ] **性能测试验收**：Benchmark 全量报告交付 + 并发压测报告交付 + 性能调优指南交付
- [ ] **完整交付物**：
  - 源代码（服务器 + 桌面客户端 + CLI 工具）
  - 16 份设计文档 + 用户手册
  - 200+ 单元测试 + 集成测试 + Benchmark 测试
  - Docker 开发环境配置 + CI/CD 流水线
  - Git tag v1.0.0
