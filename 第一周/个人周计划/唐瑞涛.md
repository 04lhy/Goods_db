# 数据库系统小学期第一周周计划 — 唐瑞涛（项目骨架 / 工具链 / 通用组件）

1. 阅读 BusTub 的 `CMakeLists.txt` 和目录结构设计，创建 goods_db 工程骨架：`src/`、`src/include/`、`test/`、`third_party/`、`tools/`、`build_support/` 完整目录；编写 CMakeLists.txt（C++17 + Clang 15 + Sanitizer + 模块化 add_subdirectory）；从 BusTub 引入 fmt、googletest、argparse、linenoise、libpg_query 等第三方库，配置 Google Test 和 Docker 开发环境。**周一必须交付可编译的空骨架，确保全组能够 `cmake .. && make`。**
2. 参考 BusTub 的 `common/` 目录，实现通用组件库：RWLatch 读写锁（基于 pthread_rwlock 或 shared_mutex）、Config 配置管理类（从文件读取 buffer_pool_size、page_size、data_directory 等参数）、日志宏（LOG_INFO / LOG_WARN / LOG_ERROR）和断言宏、编译期常量定义（PAGE_SIZE / BUFFER_POOL_DEFAULT_SIZE / MAX_TUPLE_SIZE），以及 Cast / HashUtil / StringUtil 等工具函数，全部配备单元测试。
3. 设计并实现 Catalog 元数据管理类（`map<table_name, TableInfo>` 维护 schema + 文件路径 + 索引列表），实现 `CREATE TABLE` 时元数据持久化到 `goods_db_catalog.db`；配置 GitHub Actions CI 流水线（push 触发 → 编译 + 全量单测 + clang-format/clang-tidy 检查）。
4. 辅助 肖清峰完成子模块链接和编译问题排查，为 刘晗阳构建性能 Benchmark 框架（Google Benchmark 或计时宏封装），确保 `make format` / `make check-lint` / `make check-clang-tidy` 全组通过。
5. 编写阶段一环境文档：Docker 启动流程、构建命令速查、目录结构说明图、CMake 模块依赖关系说明，确保后续阶段新成员可快速上手。
