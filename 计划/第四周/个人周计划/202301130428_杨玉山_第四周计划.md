# 数据库系统小学期第四周周计划 — 杨玉山（表与元组层 / 新零售数据模型）

1. 编写新零售场景完整 DDL 建表脚本 new_retail_schema.sql——warehouses / products / inventory / customers / orders / order_items / shipments 7 张表，包含完整列定义、主键/外键/UNIQUE/NOT NULL 约束、7 个推荐索引（4 个 Hash + 3 个 B+Tree）的 CREATE INDEX 语句。
2. 编写批量测试数据生成器 tools/data_gen/generate_test_data.cpp——生成 50 个仓库 + 1000 种商品 + 20 万条库存 + 5 万条客户 + 10 万条订单 + 50 万条订单明细 + 10 万条物流记录，输出为批量 INSERT SQL 文件（每 100 行一批加快加载），加载后验证数据完整性（行数校验/索引可用性/Catalog 一致性）。
3. 执行全部 7 大新零售业务查询测试——库存实时查询 / 多仓库库存汇总 / 订单物流 JOIN / 客户消费排行 / 库存预警 / 销售趋势分析（窗口函数）/ 复杂 5 表 JOIN。每个查询记录 SQL + EXPLAIN 输出 + 执行耗时(ms) + 返回行数 + 使用的索引。
4. 输出新零售业务查询测试报告——标注性能瓶颈查询和优化建议（增加索引/改写 SQL/调整 JOIN 顺序），输出新零售场景完整文档（数据模型 ER 图 + 索引选型方案 + 业务查询 SQL 模板库 + 数据加载脚本使用说明）。
5. 编写面向用户的示例场景脚本——完整的采购→仓储→销售→物流"故事线"SQL 脚本，帮助验收人员理解新零售业务的数据库操作流程。
6. 协助肖清峰进行全系统集成测试中新零售数据场景的验证，参与全组最终验收演示（负责新零售业务 Demo 操作和讲解）。
